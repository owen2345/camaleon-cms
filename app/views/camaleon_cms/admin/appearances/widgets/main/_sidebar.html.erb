<div id="content-sidebar" class="col-md-4">
    <a href="<%= new_cama_admin_appearances_widgets_sidebar_path %>" title="<%= t('camaleon_cms.admin.widgets.create_sidebar') %>" class="ajax_link btn btn-secondary" ><%= t('camaleon_cms.admin.widgets.create_sidebar') %></a>
    <div class="card card-default">
        <div class="card-body">
            <div class="row">
                <% current_site.sidebars.each do |sidebar| %>
                    <div class="item-sidebar card card-default card-toggled" data-id="<%= sidebar.id %>">
                        <div class="card-header">
                            <h3 class="card-title">
                                <%= sidebar.name %><br>
                                <small class="text-muted"><%= sidebar.description %></small>
                            </h3>
                            <ul class="panel-controls">
                                <li><a href="<%= edit_cama_admin_appearances_widgets_sidebar_path(sidebar) %>" class="panel-edit ajax_link" title="<%= t('camaleon_cms.admin.button.edit')%>"><span class="fa fas fa-pen-to-square"></span></a></li>
                                <li><%= link_to raw('<span class="fas fa-xmark text-danger"></span>'), cama_admin_appearances_widgets_sidebar_path(sidebar), method: :delete, data: { confirm: t('camaleon_cms.admin.message.delete_item') }, class: "panel-delete", title: "#{t('camaleon_cms.admin.button.delete')}" %></li>
                                <li><a class="panel-collapse" href="#"><span class="fas fa-angle-down"></span></a></li>
                            </ul>
                        </div>
                        <div class="sidebar-body card-body" data-id="<%= sidebar.id %>">
                            <% sidebar.assigned.each do |assign| %>
                                <%= render partial: "camaleon_cms/admin/appearances/widgets/assign/form", locals: {sidebar: sidebar, assigned: assign, widget: assign.widget} %>
                            <% end %>
                        </div>
                    </div>
                <% end %>
            </div>
        </div>
    </div>
</div>