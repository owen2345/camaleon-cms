<script>jQuery(function(){
    var panel = $("#comments_answer_list");
    panel.find(".ajax_modal").ajax_modal({ callback: function(modal){ modal.find("form").validate();  } });
    $("#sidebar-menu li[data-key='comments']").addClass("active"); //fix menu
})</script>
<div class="card card-default" id="comments_answer_list">
    <div class="card-header">
        <div class="row">
            <div class="col-md-12">
                <h4 class="float-left"><span class="fas fa-comments"></span> <%= t('camaleon_cms.admin.post_type.comments') %></h4>
                <%= link_to raw("<i class='fas fa-plus'></i> #{t('camaleon_cms.admin.button.new')}"), {action: :new}, class: "btn btn-primary float-right ajax_modal", title: t("camaleon_cms.admin.comments.new_msg") %>
            </div>
        </div>
    </div>
    <div class="card-body">
        <div class="messages messages-img">
            <%= raw "<div class='alert alert-warning'>#{t('camaleon_cms.admin.comments.message.there_no_comments')}</div>" if @comments.size == 0 %>
            <%= cama_comments_render_html(@comments) %>
        </div>
    </div>
</div>
