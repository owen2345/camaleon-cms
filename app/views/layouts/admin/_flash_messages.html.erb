
<% if flash[:notice] %>
    <div class="flash_messages">
        <div class="alert alert-success">
            <button type="button" class="close" data-dismiss="alert" aria-label="Close"><span aria-hidden="true">&times;</span></button>
            <%= raw(flash[:notice]) %>
        </div>
    </div>
<% end %>
<% if params[:notice] %>
    <div class="flash_messages">
        <div class="alert alert-success">
            <button type="button" class="close" data-dismiss="alert" aria-label="Close"><span aria-hidden="true">&times;</span></button>
            <%= raw(params[:notice]) %>
        </div>
    </div>
<% end %>

<% if flash[:error] %>
    <div class="flash_messages">
        <div class="alert alert-danger">
            <button type="button" class="close" data-dismiss="alert" aria-label="Close"><span aria-hidden="true">&times;</span></button>
            <%= raw(flash[:error]) %>
        </div>
    </div>
<% end %>

<% if flash[:alert] %>
    <div class="flash_messages">
        <div class="alert alert-warning">
            <button type="button" class="close" data-dismiss="alert" aria-label="Close"><span aria-hidden="true">&times;</span></button>
            <%= raw(flash[:alert]) %>
        </div>
    </div>
<% end %>


<style>
    .flash_messages{
        margin-left: 10%;
        margin-right: 10%;
        width: 80%;
    }
    .template-admin .flash_messages{
        display: inline-block;
        text-align: center;
    }
</style>

